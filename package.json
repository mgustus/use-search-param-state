{
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "yarn workspace use-search-param-state build",
    "test": "yarn workspace use-search-param-state test",
    "lint": "yarn workspace use-search-param-state lint",
    "typecheck": "yarn workspaces run typecheck",
    "release": "yarn workspace use-search-param-state release",
    "dev:react": "yarn workspace react-arktype-example dev",
    "dev:react-zod": "yarn workspace react-zod-example dev",
    "dev:next": "yarn workspace nextjs-example dev",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "standard-version": "^9.5.0"
  }
}
